package io.github.andrewmalitchuk.uncial.samples.android.core.asset

/**
 * The two fixtures shipped in `assets/`, which are the sample's whole point.
 *
 * One is image-only and one carries a text layer, so the same UI can demonstrate both
 * paths: the scan starts the engine, the digital PDF never does.
 */
internal const val BUNDLED_SCAN: String = "sample-scan.pdf"
